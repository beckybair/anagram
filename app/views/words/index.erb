<div id="show-list">
  <h1>Words</h1>
  <p>If you don't see a word listed here you can add them <a href="/words/new">here</a>.
  <p>If you want to go back to the beginning, click <a href="/">here</a>.
  
  <table class="table table-striped">
    <thead>
      <th>Text</th>
      <th>Letters</th>
      <th>Edit</th>
      <th>Delete</th>    
    <tbody>
      <% @words.each do |word| %>
        <tr>
          <td>
            <a href="<%= "/words/#{word.id}" %>"><%= "#{word.text}" %></a>
          </td>
          <td><%= "#{word.letters}" %></td>        
          <td>
            <a href="<%= "/words/#{word.id}/edit" %>">Edit</a>
          </td>
          <td> 
            <form action="<%= "/words/#{word.id}" %>" method="post">
              <input type="hidden" name="_method" value="delete" />
              <button id="btn-delete" class="btn btn-danger" type="submit">
                <i class="fa fa-exclamation-triangle"></i>Delete
              </button>
            </form>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>